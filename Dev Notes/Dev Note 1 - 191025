Date: October 19, 2025
Project: wedding-site

✅ Current State

Frontend: Fully working React + Vite + Tailwind setup.

Backend: Folder structure and Express server created.

Prisma schema: Defined for User, Rsvp, and Photo.

Environment setup: .env file ready for database connection.

Error encountered:

Error: P1001 Can't reach database server at `localhost:51214`


→ Database server not yet installed or running.

🧭 Next Steps

Install PostgreSQL locally (or use Supabase/Neon).

After install, create database:

createdb weddingdb


Update .env file:

DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/weddingdb?schema=public"


Run migrations:

npx prisma migrate dev --name init


Start backend server:

npx ts-node-dev src/index.ts


Next development tasks:

Add authentication (JWT login for couple + invite code for guests)

Create /api/auth and /api/guests routes

Connect frontend to backend RSVP endpoint